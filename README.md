# Windows VM for Ubuntu

Follow instructions [here](https://raw.githubusercontent.com/Fmstrat/winapps/main/docs/KVM.md).

Use this script to install the VM (named `office` in `start.sh`) as an application.

Run `./install.sh`

